# Secrets — never upload these
.env

# Local backups (may contain old secrets) — never upload
_old_local_backup_*/

# Python junk
__pycache__/
*.pyc
.venv/

# macOS junk
.DS_Store

# Generated audio (built fresh each run, not checked in)
*.mp3
*.ogg
